def most_frequent(string=" "):
    d = dict()
    for key in string:
        if key not in d:
            d[key] = 1
        else:
            d[key] += 1
    lst = []
    lst.append(d)
    lst.sort()
    print(lst)


most_frequent()
